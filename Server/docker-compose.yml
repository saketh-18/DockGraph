version: "3.8"
services:
  ekg:
    build: .
    volumes:
      - ./:/app
    stdin_open: true
    tty: true
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
    command: ["python", "-m", "chat.cli"]
